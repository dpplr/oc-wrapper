# Wrapper for openshift-cli
Wraps `oc project <project-name>` to set `$TILLER_NAMESPACE=<project-name>`

## zplug
If you are using zplug add the following to your `.zshrc`
`zplug "dpplr/oc-wrapper", use:oc.zsh`

